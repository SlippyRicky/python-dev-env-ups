import math
import numpy as np
import cmath

math.pi
pow(math.sin(2.5), 2) + pow(math.cos(2.5), 2)
pow(np.sin(2.5), 2) + pow(np.cos(2.5), 2)
math.exp(math.log(3))
# math.sqrt(-1) # Produit une ValueError
cmath.sqrt(-1)
np.sqrt(-1) #Fonctionne sur Linux mais RuntimeWarning sur OSx
